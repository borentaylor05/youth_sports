<% @parent_comments.each do |comment| %>
<li>
	<span class="body"><%= comment.body %></span>
	<span class="timestamp text-muted">Posted <%= time_ago_in_words(comment.created_at) %> ago</span>

</li>
<% end %>
